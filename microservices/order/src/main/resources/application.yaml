server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: megatron
  jpa:
    hibernate:
      ddl-auto: 'drop-and-create'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

